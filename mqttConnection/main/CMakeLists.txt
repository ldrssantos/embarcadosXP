idf_component_register(
    SRCS "main.c"
    INCLUDE_DIRS ""
)

set(COMPONENT_REQUIRES mqtt)

# set(COMPONENT_SRCS "main.c wifi_app_main.c")
# set(COMPONENT_ADD_INCLUDEDIRS ".")

# register_component()
